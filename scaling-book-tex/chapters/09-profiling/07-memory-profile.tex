\subsection{Memory Profile}

The Memory Profile makes it easy to see the program memory as a function of time. This is helpful for debugging OOMs. You can see here about 7.5GB allocated to model parameters and about 10GB free. So we can fit a lot more into memory.

\begin{figure}[htb]
\centering
\includegraphics[width=\textwidth]{images/memory-viewer.png}
\caption{Memory Profile showing memory allocation over time, with approximately 7.5GB allocated to model parameters and 10GB free memory remaining.}
\label{fig:memory-viewer}
\end{figure}
